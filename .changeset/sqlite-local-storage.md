---
"@taskcast/sqlite": minor
"@taskcast/cli": minor
---

Add SQLite local storage adapter for zero-dependency development. Use `taskcast start --storage sqlite` to persist data locally without Redis or PostgreSQL.
