server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location = /api/config {
        default_type application/json;
        alias /usr/share/nginx/html/api-config.json;
    }
}
